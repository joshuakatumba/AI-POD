version: "3.9"

services:
  app_blue:
    build: .
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    container_name: app_blue
    command: >
      gunicorn --chdir /usr/src/app core.asgi:application
      -k uvicorn.workers.UvicornWorker
      --bind 0.0.0.0:8000
      --workers 2
    environment:
      - DEBUG=0
      - DJANGO_SETTINGS_MODULE=core.settings.staging
    ports:
      - "8001:8000"
    volumes:
      - static_volume:/usr/src/app/core/staticfiles
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthz/"]
      interval: 10s
      timeout: 5s
      retries: 5

  app_green:
    build: .
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    container_name: app_green
    command: >
      gunicorn --chdir /usr/src/app core.asgi:application
      -k uvicorn.workers.UvicornWorker
      --bind 0.0.0.0:8000
      --workers 2
    environment:
      - DEBUG=0
      - DJANGO_SETTINGS_MODULE=core.settings.staging
    ports:
      - "8002:8000"
    volumes:
      - static_volume:/usr/src/app/core/staticfiles
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthz/"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  static_volume: