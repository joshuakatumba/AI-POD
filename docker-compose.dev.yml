services:
  app:
    command: >
      gunicorn --chdir /usr/src/app core.asgi:application
      -k uvicorn.workers.UvicornWorker
      --bind 0.0.0.0:8000
      --workers 1
      --reload
    volumes:
      - ./:/usr/src/app
    environment:
      - DEBUG=1